import LogoutButton from "@/components/LogoutButton";

const EditorPage = () => {
    return (
        <div style={{ padding: 20 }}>
            <h1>📝 Editor Page</h1>
            <p>Bienvenido al panel del editor.</p>
            <LogoutButton />
        </div>
    );
};

export default EditorPage;
